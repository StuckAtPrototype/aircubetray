# Runtime dependencies (Windows, Linux, macOS)
PyQt6>=6.4
pyserial>=3.5
bleak>=0.21
esptool>=4.7
requests>=2.31

# Build (produces dist/AirCubeTray.exe; Inno Setup is a separate install)
# pyinstaller>=6.0

# Dev / icon generation (only needed to regenerate aircube_tray.ico)
# Pillow>=10.0
